import Phaser from 'phaser';
import GameConfig = Phaser.Types.Core.GameConfig;

const config : GameConfig = {
    type: Phaser.AUTO,
    //parent: 'game',
    backgroundColor: '#de77d8',
    width: "100%",
    height: "100%",

};
export default config;